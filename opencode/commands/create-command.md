---
description: Creates or adjusts an OpenCode slash command
agent: orchestrator
---

Create or update an OpenCode command using `$ARGUMENTS` as the initial brief.

Workflow:
1. Review `commands/` and the related agents.
2. Define the goal, placeholders, target agent, and interaction level.
3. Prefer short, reusable commands with clear descriptions.
4. Edit the final file and explain which new workflow it enables.
